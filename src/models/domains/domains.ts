export interface DNSEntry {
  id?: string;
  domain?: string;
  vertical?: string;
  shop_id?: string;
  is_primary?: boolean;
  locale?: string;
  locale_only?: boolean;
  strictly_local_only?: boolean;
  tld?: string;
  is_system?: boolean;
  is_active?: boolean;
  theme_id?: string;
}
